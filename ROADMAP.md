# 开发路线图

规划中的功能，按优先级排序。每条含：目标 / 可复用的现有基础设施 / 技术要点 /
规模预估 / 验收标准。

**如何更新本文件**：完成一条 → 实现细节写入 DEVLOG.md 并勾掉本条；新想法 →
追加条目并注明优先级理由；优先级调整需与用户确认。

---

## ✅ 1. 全文搜索（文档内 FTS，含 OCR 页）— 2026-08-16 完成

**目标**：在整本书/论文里搜索词或词组，高亮结果并跳页。500 页扫描书可检索。

**现有基础**：
- `fts_documents` FTS5 虚拟表已建（src-tauri/src/db.rs），未接前端
- 页面跳转已有 `_scrollTarget` 机制（ReaderViewport）
- OCR 引擎已提速至 ~0.4s/页，批量 OCR 可行性成立

**技术要点**：
- 文字版页搜内嵌文本层（getTextContent），扫描页搜 `ocr_cache`
- 未 OCR 的页：搜索前/中后台批量 OCR（显示进度；~0.4s/页）
- 结果高亮 + 点击跳页；不区分大小写、支持词组
- 顺带解决遗留项"搜索不区分大小写、无正则支持"

**规模**：中等（前端搜索面板 + Rust 查询命令 + 后台批量 OCR 任务）。

**验收**：扫描书搜索术语 → 结果列表 → 点击跳转并高亮；文字版 PDF 同样可用。

> ✅ 已完成，实现细节与踩坑见 DEVLOG.md 2026-08-16「全文搜索（FTS）落地」。

---

## 🥈 2. 翻页方式：左右滑动模式 + 页间分隔线

**目标**：在连续滚动之外新增横向"左右滑动"翻页模式；两种模式下都渲染
分隔线，让页面边界清晰可辨（连续滚动模式现有 shadow 较淡）。

**现有基础**：
- ReaderViewport 虚拟化 + pageHeights + `_scrollTarget` 跳转机制
- appStore 已有 `layoutMode: 'single' | 'scroll'` 枚举（single 语义未接线）
- 手动缩放的"滚动偏移居中"（scrollLeft 居中、两侧可达）可复用于
  横向单页容器的左右切换

**技术要点**：
- 横向模式：每页一个横向 slot（宽 = 阅读区宽），内容居中；←/→ 键、
  触摸板横向手势、滚动吸附翻页；activePage 同步与阅读进度持久化
  复用现有 scroll 统计逻辑
- 页间分隔线：两种模式统一在页面 wrapper 上渲染分隔条（渐变阴影/
  细线 + 间距），替换或强化现有 `shadow-lg`

**规模**：小到中（布局模式接线 + 分隔线样式 + 手势）。

**验收**：左右滑动/方向键平滑翻页、阅读进度正确；两种模式下页面边界
一眼可辨。

---

## 🥉 3. 大 PDF 导航（缩略图侧栏 / 快速定位）

**目标**：长文档的缩略图侧栏——滚动高亮当前页、点击跳转、拖动快速翻页。

**现有基础**：
- IntersectionObserver 虚拟化渲染 + pageHeights + scrollTarget
  （src/components/ReaderViewport.tsx）
- PDF.js 渲染管线可低 DPI 复用

**技术要点**：
- 缩略图按需渲染（可见窗口 + 缓冲，低 DPI canvas）
- 当前页高亮、点击跳转（scrollTarget）、拖动滑块快速定位

**规模**：中等（侧栏组件 + 缩略图渲染）。

**验收**：500 页书快速定位到任意页；滚动时缩略图跟随高亮。

---

## 4. 翻译（本地小体积，两阶段）

**目标**：选中句子/段落即时查看中文翻译，全离线。

**v1 词典拼接（0MB，先行）**：
- 选中文本 → 逐词 ECDICT 查词 + 现有词形还原（lemmatisation/exchange 已在
  dictionary.rs 实现）→ 术语级注释式翻译
- 右键菜单加"翻译"项 + 翻译结果显示面板
- 零新依赖、立即可用；解决"学术术语不懂"

**v2 本地神经翻译（~90MB）**：
- 模型：Helsinki-NLP opus-mt-en-zh 转 ONNX **int8 动态量化**
  （fp32 ~300MB → ~90MB；BLEU 27.90 → 27.65，几乎无损；
  CPU 推理每句 <1s）
- **复用现有 ort load-dynamic 运行时**（src-tauri 已集成 onnxruntime），
  无需新运行库
- 按句切分输入；新增 translation_cache 表（text_hash 思路同 tts_cache）
- 解决"整句读不懂"

**备选方案（暂不采用）**：
- CTranslate2 int8（~100MB、推理更快）——需 C++ FFI，工程量大
- 蒸馏小模型（~30–60MB）——质量下降明显、社区模型可用性参差

**规模**：v1 小；v2 中（模型转换/下载脚本 + 推理命令 + 缓存 + UI）。

**验收**：v1 选中学术句子 → 术语级注释翻译；v2 整句流畅中文，
模型文件 ≤ ~100MB，翻译延迟 <1s/句。

---

## 5. SRS 词汇复习

**目标**：闭环"划词 → 加入词汇库 → 定期复习"，间隔重复记忆。

**现有基础**：vocabulary 表已有 `review_count` / `last_review_at` 字段。

**技术要点**：复习面板 + SM-2 简单算法；按到期时间取词。

**规模**：小。

**验收**：加入的词汇按间隔出现，答错重排、答对推迟。

---

## 6. 标注导出（Markdown/Anki）

**目标**：高亮 + 笔记导出为 Markdown 读书笔记（可选 Anki 卡片）。

**现有基础**：annotations 表完整（高亮+笔记，按文档/页存储）。

**技术要点**：按书聚合导出 Markdown；Anki 导出可选。

**规模**：小。

**验收**：导出笔记含原文引用、页码、笔记内容，格式整洁。

---

## 7. TTS 朗读（继续后置）

**目标**：选中段落朗读，播放/暂停/调速/音色。

**现有基础**：`tts_cache` 表已建；appStore 占位状态（ttsPlaying/ttsRate/ttsVoice）。

**技术要点**：Kokoro-82M（~300MB）ONNX 集成 + Web Audio 播放。

**规模**：大，使用频率相对低，最后做。

---

## 暂缓备用

- DOI 元数据提取（Crossref API——破坏全离线原则，需用户确认）
- 倾斜页显示层矫正（OCR 已内部矫正；UI 方案已评估，暂缓）
- OCR 性能余项：倾斜角便宜估算器 / DirectML / 页级并行（引擎 0.4s 已非瓶颈）
